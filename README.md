# Reverse-Shell-and-RAT
This project demonstrates a basic client-server architecture designed for remote command execution and file transfers using Python's socket library. It includes an Attacker Console to dispatch commands and a Victim Shell that executes terminal instructions and returns base64-encoded file data.
